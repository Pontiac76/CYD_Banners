# Agent Instructions for This Project

Before making changes, read `MEMORY.md` if it exists. Treat it as the project-local memory bank for prior decisions, user preferences, and current status.

When you learn something durable about this project, update `MEMORY.md` before finishing. Keep it concise and factual.

Do not use `MEMORY.md` as a task dump for every transient thought. Store only details that should survive between coding sessions.
